# Audio Recorder

Records microphone audio, shared tab/system audio, or both mixed together. The latest recording is exposed through `describeCurrentAssets` so the dashboard Tools workspace can send it to other tools or import flows.
