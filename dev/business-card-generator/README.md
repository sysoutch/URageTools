# Business Card Designer

A self-contained dashboard tool for designing front and back business-card faces with Fabric.js. It supports text, uploaded logos, QR codes, solid or gradient backgrounds, and high-resolution PNG export.

The tool keeps its runtime and vendor assets inside this directory. Dashboard integration is provided by the shared theme, current-output, and tool bridge scripts referenced by `index.html`.
